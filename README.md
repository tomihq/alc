### 1. Creación de Arrays

1. `np.array()`
2. `np.zeros()`
3. `np.ones()`
4. `np.eye()`

### 2. Secuencias

5. `np.arange()`
6. `np.linspace()`

### 3. Estructura y Copia

7. `np.shape()`
8. `np.ndim()`
9. `np.reshape()`
10. `np.copy()`

### 4. Operaciones

11. `np.cos()`
12. `np.sen()` *(aunque en NumPy realmente es `np.sin()`)*
13. `np.random`

### 5. Submódulo de aleatoriedad

14. `np.random` *(como submódulo; probablemente acá entran las funciones de aleatoriedad que les hayan enseñado)*

### 6. Otras operaciones

15. **Slicing**
16. **Operaciones estándar** (`+`, `-`, `*`, `/`, etc.)
17. `@` — producto matricial
18. `np.isclose()` — comparación con tolerancia
